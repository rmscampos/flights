<%- include('../partials/header') %>
<table id="list">
  <thead>
    <tr>
      <th>Airline</th>
      <th>Flight Number</th>
      <th>Departs</th>
    </tr>
  </thead>
  <tbody>
    <% flights.forEach(function(f) { %>
      <tr>
        <td><%= f.airline %></td>
        <td><%= f.flightNumber %></td>
        <td><%= f.departs %></td>
      </tr>
      <% }); %>
    </tbody>
    
  </table>
  
<%- include('../partials/footers') %>
